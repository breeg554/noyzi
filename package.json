{
	"name": "meshy",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"dev": "bun run --filter web dev",
		"build": "bun run --filter @meshy/core build && bun run --filter @meshy/react build && bun run --filter web build",
		"test": "bun run --filter '@meshy/*' test",
		"check": "biome check .",
		"format": "biome format --write .",
		"lint": "biome lint ."
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.5"
	}
}
